# Copy to .env and fill in. Never commit the real .env.

# Required for the discovery run (LLM-driven agent loop).
OPENAI_API_KEY=sk-...

# Optional overrides.
OPENAI_MODEL=gpt-4o
MOCK_APP_PORT=4000
CONTROL_SERVER_PORT=4100
